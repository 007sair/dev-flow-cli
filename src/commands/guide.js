const {  intro, outro, note } = require('../utils');

function guide() {
  intro('🌊 Dev Flow CLI 使用规范');

  note(`基于 aicommits (v2.0.0+) 提供的 AI 辅助提交功能。
- 支持自动生成 Commit Message        
- 支持复制到剪贴板或直接提交
- 支持自定义 Prompt 模板
- 配置命令: flow ai setup`, '🤖 阶段 0：AI 智能提交 (ai-commit)');

  note(`将个人开发分支合并到公共特性分支。
1. 检查工作区状态
2. 选择个人开发分支
3. 选择目标公共特性分支 (feat/*)
4. 变基/合并并推送`, '📌 阶段 1：特性同步 (feature-sync)');

  note(`从公共特性分支创建 Release 分支，准备发版。
1. 选择公共特性分支
2. 确定发布版本号 (Major/Minor/Patch)
3. 创建 release/v* 分支并推送`, '📌 阶段 2：预发布 (pre-release)');

  note(`完成发布流程，生成 Changelog 并合并到主分支。
1. 选择 release 分支
2. 运行 standard-version 生成 Changelog 和 Tag
3. 推送并提示创建 PR (release -> master)`, '📌 阶段 3：正式发布 (release-finish)');

  outro('提示：运行 flow 命令开始交互式流程');
}

module.exports = guide;
